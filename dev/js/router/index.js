import Vue from "vue";
import Router from "vue-router";
//import store from "../store";

Vue.use(Router);

const router = new Router({
  mode: "history",
  routes: [
    // {
    //   path: "/",
    //   name: "client",
    //   component: Client
    // }
  ]
});

export default router;